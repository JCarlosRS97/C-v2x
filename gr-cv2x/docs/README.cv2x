This is the cv2x-write-a-block package meant as a guide to building
out-of-tree packages. To use the cv2x blocks, the Python namespaces
is in 'cv2x', which is imported as:

    import cv2x

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(cv2x)
